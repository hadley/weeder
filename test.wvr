\documentclass[11pt]{report}
\usepackage{fullpage}
\usepackage{graphicx}
\usepackage{moreverb}
\title{Weaver test}
\author{Hadley Wickham}
\date{\today}

\begin{document}

\maketitle

<% library(lattice) %>

\chapter{Introduction}

This latex document will be the testing ground for weaver.

\begin{figure}[h!]
<%=g xyplot(runif(100) ~ rnorm(100)) %>
\caption{Uniform vs. normal.}
\label{est:together}
\end{figure}

<%= 
 data(tli)
 
 ## Demonstrate data.frame
 tli.table <- xtable(tli[1:20,])
 digits(tli.table)[c(2,6)] <- 0
 print(tli.table)
%>

<% 
  a <- runif(100)
%>

<%=r
	data(tli)
	mod <- lm(tlimth ~ grade, tli)
	summary(tli)
%>

\end{document}